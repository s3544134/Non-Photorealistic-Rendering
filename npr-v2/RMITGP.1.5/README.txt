Core system library, the code in this directory compiles into a
file librmitgp.a.

To compile the library do the following.

a) Change any parameters in the file Makefile to suit your platform
b) type `make depend' 
c) type `make clean'
d) type `make'
